{% extends 'base.html.twig' %}

{% block title %}New Categories{% endblock %}

{% block body %}
    <div class="container">
        <div class="mx-auto mb-3">
            <h4>Création nouvelle catégorie</h4>

        {{ include('categories/_form.html.twig') }}

        <div class="container">
            <div class="mx-auto">
                 <a href="{{ path('app_categories_index') }}" class="btn btn-primary">Voir la liste</a>
            </div>
        </div>
        
        </div>
    </div>
{% endblock %}
